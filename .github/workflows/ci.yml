name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend:
    name: Backend — lint & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: thread_mesh_inspector/backend/requirements.txt

      - name: Install dependencies
        run: pip install -r thread_mesh_inspector/backend/requirements.txt pytest pytest-asyncio

      - name: Run tests
        run: pytest thread_mesh_inspector/backend/tests/ -v --tb=short
        env:
          TMI_MOCK_MODE: "true"
          TMI_DATA_DIR: "/tmp"

  frontend:
    name: Frontend — build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: thread_mesh_inspector/frontend
        run: npm install

      - name: Type check
        working-directory: thread_mesh_inspector/frontend
        run: npm run type-check

      - name: Build
        working-directory: thread_mesh_inspector/frontend
        run: npm run build
